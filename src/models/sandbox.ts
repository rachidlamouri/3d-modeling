import { RectangularPrism } from '../modeling';

export default {
  sandbox: new RectangularPrism({
    origin: ['center', 'center', 'bottom'],
    lengthX: 20,
    lengthY: 16,
    lengthZ: 10,
  }),
};
